import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.whistlingwombatflip',
  appName: 'whistling-wombat-flip',
  webDir: 'dist'
};

export default config;
